<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.termproject.mybookshelf">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backupscheme"
        android:icon="@mipmap/ic_launcher"
        android:label="Ha Eun's BookShelf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.termproject.mybookshelf.MainActivity">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity android:name=".Splash"></activity>
        <activity
            android:name="com.termproject.mybookshelf.SingleAddActivity"
            android:theme="@style/AppOverlayTheme" />
        <activity
            android:name="com.termproject.mybookshelf.BookEditActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name="com.termproject.mybookshelf.BookDetailActivity"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Sliding.Light" />
        <activity android:name="com.termproject.mybookshelf.AboutActivity" />
        <activity android:name="com.termproject.mybookshelf.SettingsActivity" />
        <activity android:name="com.termproject.mybookshelf.BatchAddActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />

        </provider>

    </application>

</manifest>